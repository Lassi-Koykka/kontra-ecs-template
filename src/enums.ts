export enum Align {
  START,
  CENTER,
  END,
}

export enum EntityTag {
  PLAYER,
  PROJECTILE,
  ENEMY,
}

export enum EntityStatus {
  FRIENDLY,
  NEUTRAL,
  ENEMY,
}

export enum ColliderType {
  RECTANGLE,
  CIRCLE,
}
